<p id="notice"><%= notice %></p>

<h1>Dots</h1>

<table>
  <thead>
    <tr>
      <th>Experiment</th>
      <th>Metric</th>
      <th>Value</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @dots.each do |dot| %>
      <tr>
        <td><%= dot.experiment_id %></td>
        <td><%= dot.metric_id %></td>
        <td><%= dot.value %></td>
        <td><%= link_to 'Show', dot %></td>
        <td><%= link_to 'Edit', edit_dot_path(dot) %></td>
        <td><%= link_to 'Destroy', dot, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Dot', new_dot_path %>
